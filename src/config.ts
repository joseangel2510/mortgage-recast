/** Single place to change once the real domain is purchased. */
export const SITE_URL = 'https://recastwise.com';
export const SITE_NAME = 'RecastWise';
export const SITE_TAGLINE = 'See if recasting your mortgage actually pays off';
export const CONTACT_EMAIL = 'contact@recastwise.com'; // update after domain purchase

/**
 * Google AdSense publisher ID, e.g. 'ca-pub-1234567890123456'.
 * Leave empty until AdSense approval — ad slots and the AdSense <script>
 * render only when this is non-empty.
 */
export const ADSENSE_CLIENT_ID = '';
